# Bibliotheques precompilees

Rempli automatiquement par la CI GitHub (voir .github/workflows/build.yml).

    android/arm64-v8a/libsyro.so
    android/armeabi-v7a/libsyro.so

En local, suis native/README.md. Ces fichiers ne sont pas versionnes
(voir .gitignore) : ils sont reconstruits a chaque build.
